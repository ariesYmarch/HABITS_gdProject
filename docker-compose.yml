version: '3.8'

services:
  # 백엔드 서버 (FastAPI) - 로컬 개발용 (Supabase DB 사용)
  backend:
    build: ./backend
    command: uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload
    volumes:
      - ./backend:/app
    ports:
      - "8000:8000"
    env_file:
      - ./backend/.env
